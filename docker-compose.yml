services:
  site-pdv:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: site_pdv_teltech
    restart: unless-stopped
    ports:
      - "3535:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOST=0.0.0.0
